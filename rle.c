#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "compress.h"
#include "rle.h"

size_t RLE( const unsigned char *input,
               unsigned char *output,
               size_t        len ) {

    printf( "RLE\n\n" );

    size_t size = 0;

    for( int i = 0; i < len; i++ ) {
        
    }

    return size;
}
